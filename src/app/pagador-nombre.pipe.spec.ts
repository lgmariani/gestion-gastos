import { PagadorNombrePipe } from './pagador-nombre.pipe';

describe('PagadorNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new PagadorNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
